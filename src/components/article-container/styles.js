import styled from 'styled-components'
import { primaryBackground } from '../../styling/colors'

export const Container = styled.div`
  width: 100%;
  padding: 1rem;
  background-color: ${primaryBackground};
`